// Package migrate offers scripts of transferring
// from older versions of resurrent filesystem
// to the one of newer version.
//
// This will include dirty works such as performing
// massive converting works on the underlying
// structure of directories. A version control
// software is recommended so that the user can
// revert to the last working version and report
// the bug to us.
package migrate
